export interface DefaultSetting {
    MENU_PLACEMENT: string,
    MENU_BEHAVIOUR: string,
    LAYOUT: string,
    RADIUS: string,
    COLOR: string,
    NAV_COLOR: string,
    USE_SIDEBAR: boolean,
}

export interface Props {
    title: string,
}